# 🧮 Determinants and Inverse of a Matrix (Basic Problems)

---

## 🔹 1️⃣ Determinant of a Matrix — Concept & Explanation

A **determinant** is a scalar value that can be computed from a square matrix. It helps determine whether a matrix has an inverse or not.
For a square matrix **A**, the determinant is denoted as **|A|** or **det(A)**.

### 💡 Key Points:

1. Determinants exist **only for square matrices** (n × n).
2. If **|A| = 0**, the matrix is **singular** (no inverse exists).
3. If **|A| ≠ 0**, the matrix is **non-singular** (inverse exists).

---

### 📊 Visual Representation (2×2):

```
A = [ a  b ]
    [ c  d ]

det(A) = a*d - b*c
```

---

### 🧠 Examples (2×2 Matrices)

**Example 1:**

```
A = [ 2  3 ]
    [ 1  4 ]
det(A) = 8 - 3 = 5
```

**Example 2:**

```
B = [ 5  7 ]
    [ 2  6 ]
det(B) = 30 - 14 = 16
```

**Example 3:**

```
C = [ 1  2 ]
    [ 3  4 ]
det(C) = 4 - 6 = -2
```

**Example 4 (Singular Matrix):**

```
D = [ 3  5 ]
    [ 6 10 ]
det(D) = 30 - 30 = 0
```

**Example 5:**

```
E = [ 7  2 ]
    [ 4  1 ]
det(E) = 7 - 8 = -1
```

**Example 6:**

```
F = [ 8  3 ]
    [ 2  9 ]
det(F) = 72 - 6 = 66
```

---

### 🧠 Example (3×3 Matrix)

```
A = [ a  b  c ]
    [ d  e  f ]
    [ g  h  i ]
```

```
A = [ 1  2  3 ]
    [ 0  1  4 ]
    [ 5  6  0 ]
```

Determinant formula for 3×3:

```
det(A) = a(ei - fh) - b(di - fg) + c(dh - eg)
```

Substitute values:

```
det(A) = 1*(1*0 - 4*6) - 2*(0*0 - 4*5) + 3*(0*6 - 1*5)
det(A) = -24 + 40 - 15
det(A) = 1
```

✅ Determinant = 1

---

## 🔹 2️⃣ Inverse of a Matrix — Concept & Explanation

The **inverse of a matrix** is a matrix that, when multiplied with the original matrix, gives the **identity matrix (I)**.

```
A × A⁻¹ = I
```

- Only **square matrices** with **|A| ≠ 0** have inverses.
- Inverse is useful to solve linear equations `AX = B` as `X = A⁻¹B`.

---

### 💡 Key Formula (2×2 matrix)

```
A = [ a  b ]
    [ c  d ], det(A) ≠ 0

A⁻¹ = (1/det(A)) * [ d  -b ]
                    [ -c  a ]
```

---

### 🧠 Examples (2×2 Matrices)

**Example 1:**

```
A = [ 2  3 ]
    [ 1  4 ]
det(A) = 5

A⁻¹ = (1/5) * [ 4  -3 ]
                [ -1  2 ]
     = [ 0.8  -0.6 ]
       [ -0.2  0.4 ]
```

**Example 2:**

```
B = [ 1  2 ]
    [ 3  4 ]
det(B) = -2

B⁻¹ = (1/-2) * [ 4  -2 ]
                 [ -3  1 ]
     = [ -2  1 ]
       [ 1.5 -0.5 ]
```

---

### 🧠 Example (3×3 Matrix Inverse)

```
A = [ 1  2  3 ]
    [ 0  1  4 ]
    [ 5  6  0 ]
```

**Step 1: Determinant**

```
det(A) = 1
```

**Step 2: Cofactor matrix**

```
C = [ -24   20  -5 ]
    [  18  -15   4 ]
    [   5   -4   1 ]
```

**Step 3: Adjugate (transpose of cofactor)**

```
adj(A) = [ -24   18   5 ]
           [  20  -15  -4 ]
           [  -5    4   1 ]
```

**Step 4: Inverse**

```
A⁻¹ = (1/det(A)) * adj(A)
     = 1 * adj(A)
     = [ -24   18   5 ]
       [  20  -15  -4 ]
       [  -5    4   1 ]
```

✅ This is the inverse of the 3×3 matrix.

---

### ✨ Key Notes:

- A matrix has an inverse **only if det(A) ≠ 0**.
- The **identity matrix** acts as “1” in multiplication.
- The inverse is used to solve linear systems `AX = B → X = A⁻¹B`.
- For **3×3 matrices**, the inverse uses the **adjugate/adjoint method**.

---

### 📌 Quick Reference

| Matrix     | Determinant                | Inverse                    |
| ---------- | -------------------------- | -------------------------- |
| 2×2 matrix | ad - bc                    | (1/det) \* [ d -b ; -c a ] |
| 3×3 matrix | a(ei-fh)-b(di-fg)+c(dh-eg) | (1/det) \* adj(A)          |
